"use server";

export async function doSocialLogin(action) {
  // The logic for this function will now be moved to the client
  console.log(action); // If you need this on the server, move the logging logic here
}

export async function doLogout(formData) {
  // Logout logic if you need it in the future
}
